# B464C — Semantic Accessibility Boundary

An authorized accessibility semantic tree is modeled as an untrusted PROJECTED observation surface,
not as canonical state.

Action candidates are bound to an exact semantic snapshot and node and explicitly have no execution,
Owner Policy or coordinate-fallback authority. A productive accessibility host must remain a separate
platform adapter behind platform permission, Owner Effect Policy, precondition validation and
postcondition observation.
